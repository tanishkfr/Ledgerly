**Prerequisites:**  Node.js
### ⚡ KEY_FEATURES

* **SYSTEM_INITIALIZATION:** A cinematic 2.5s "Mechanical Iris" boot sequence with real-time system logs.
* **VAULT_ENCLAVE:** High-density account management featuring a "Liquidity Runway HUD" and "Titanium-Grade" active cards.
* **EQUITIES_COMMAND:** Real-time stock telemetry with Volatility Jitter detection and Alpha_Vector comparison charting.
* **KINETIC_GLOW:** A global, cursor-centric radial border glow implemented via high-performance CSS masking.
* **BENTO_GRID_ARCHITECTURE:** Modular, responsive layout inspired by Japanese industrial design.

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
### 🛠️ TECH_STACK

* **CORE:** React 18 + TypeScript
* **ANIMATION:** Framer Motion (Spring-based physics)
* **STYLING:** Tailwind CSS + Custom CSS Variables
* **CHARTS:** Recharts (Refined with custom HUD tooltips)
* **ICONS:** Lucide React (Standardized to 1.2px stroke weight)

### 🚀 INITIALIZATION_PROTOCOL

```bash
# Clone the repository
git clone [https://github.com/your-username/ledgerly-fin-os.git](https://github.com/your-username/ledgerly-fin-os.git)

# Install dependencies
npm install

# Boot system
npm run dev
